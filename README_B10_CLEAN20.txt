PharmFlow Project B — B10 Clean20

Upload all files in this folder to the Project B repository root.
Commit subject: Reduce idle cloud polling

This release intentionally does NOT add the proposed 10-minute Idle Sleep yet.
First verify the root polling reduction independently. Idle Sleep can then be added as a separate failsafe if desired.
